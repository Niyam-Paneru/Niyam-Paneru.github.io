{
  "name": "premium-proof-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:unit": "node --test tests/*.test.mjs",
    "check:site": "node scripts/check-site.mjs",
    "check:syntax": "node --check assets/script.js && node --check assets/csv-rescue.js && node --check assets/webhook-lab.js",
    "test:browser": "node tests/browser-smoke.mjs",
    "check": "npm run test:unit && npm run check:site && npm run check:syntax"
  }
}
